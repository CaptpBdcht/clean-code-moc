{
  "name": "cc2-guillan-julien",
  "version": "1.0.0",
  "description": "```` git fetch -p                                # mettre a jour mes branches git branch -a                               # voir toutes les branches git checkout -b cc2-mon-nom                 # créer ma branche git push --set-upstream origin cc2-mon-nom  # push la remote ````",
  "main": "src/range.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "start": "tsc -p ./tsconfig.json && node build/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "coverage": "nyc -r lcov -e .ts -x \"*.test.ts\" npm run test"
  },
  "author": "Julien GUILLAN",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.2"
  },
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "nyc": "^15.1.0",
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0-pre.61",
    "typescript": "^4.0.3"
  }
}

{
  "name": "cc1",
  "version": "0.0.1",
  "description": "",
  "main": "build/index.js",
  "directories": {
    "doc": "doc"
  },
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "start": "tsc -p ./tsconfig.json && node build/index.js",
    "dev": "ts-node-dev --respawn --transpile-only index.ts",
    "bowlingDev": "ts-node-dev --respawn --transpile-only index.ts"
  },
  "author": "Julien GUILLAN",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.2"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0-pre.61",
    "typescript": "^4.0.2"
  }
}
